<aside class="character-preview">
  <div class="preview-portrait">
    <img
      id="preview-portrait"
      src="/images/classes/fighter.png"
      alt="Портрет персонажа"
    />
    <span></span>
  </div>

  <div class="preview-content">
    <span class="preview-kicker">Летопись героя</span>
    <h2 id="preview-name">Безымянный герой</h2>

    <dl>
      <div>
        <dt>Вид</dt>
        <dd id="preview-species">—</dd>
      </div>
      <div>
        <dt>Рост и вес</dt>
        <dd id="preview-physique">—</dd>
      </div>
      <div>
        <dt>Класс</dt>
        <dd id="preview-class">—</dd>
      </div>
      <div>
        <dt>Предыстория</dt>
        <dd id="preview-background">—</dd>
      </div>
    </dl>
  </div>
</aside>

<style>
  .character-preview {
    position: sticky;
    top: 5.25rem;
    min-width: 0;
    max-width: 100%;
    overflow: hidden;
  }

  .preview-portrait {
    position: relative;
    height: 285px;
    background: #17171b;
  }

  .preview-portrait img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: top center;
  }

  .preview-portrait span {
    position: absolute;
    inset: 0;
    background: linear-gradient(transparent 48%, var(--card-bg));
  }

  .preview-content {
    position: relative;
    margin-top: -1rem;
    padding: 0 1.2rem 1.3rem;
  }

  .preview-kicker {
    color: var(--wizard-gold);
    font-size: 0.68rem;
    font-weight: 900;
    letter-spacing: 0.13em;
    text-transform: uppercase;
  }

  .preview-content h2 {
    margin: 0.3rem 0 1rem;
    font-family: Georgia, serif;
    font-size: 1.55rem;
    overflow-wrap: anywhere;
  }

  .preview-content dl {
    display: grid;
    gap: 0.65rem;
  }

  .preview-content dl > div {
    display: flex;
    justify-content: space-between;
    gap: 0.8rem;
    padding-bottom: 0.45rem;
    border-bottom: 1px solid var(--card-border);
  }

  .preview-content dt {
    opacity: 0.52;
  }

  .preview-content dd {
    min-width: 0;
    margin: 0;
    font-weight: 700;
    text-align: right;
    overflow-wrap: anywhere;
  }
</style>
